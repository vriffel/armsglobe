## Funções: Exemplos resolvidos

## Somas
1:50 +  1:50
ret <- NULL
for (i in c(1:50)) {
    a <- 1:50
    b <- 1:50
    ret[i] <- a[i] + b[i]
}

## Escreva uma função que calule o fatorial de um número inteiro positivo.

fac <- function(n) {
    if (n ==  0 | n == 1) {
        return(n)
    }else {
        n <- n * fac(n - 1)
        return(n)
    }
}
